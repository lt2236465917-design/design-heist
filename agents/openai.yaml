interface:
  display_name: "Design Heist"
  short_description: "Authorized web clone from URL, screenshots, or video"
  default_prompt: "Use $design-heist to reconstruct this authorized webpage from a URL, screenshots, or a screen recording, then build and verify the clone."
